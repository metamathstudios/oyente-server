{
  "name": "@metamath/oyente",
  "version": "1.0.0",
  "description": "Blockchain event listeners used by MetaMath projects",
  "main": "index.js",
  "author": "Phill \"Taurinos\" Menezes",
  "scripts": {
    "lint": "standard .",
    "lint:fix": "standard . --fix",
    "test": "jest --verbose",
    "test:watch": "jest --watchAll --verbose",
    "coverage": "jest --watchAll --verbose --coverage"
  },
  "standard": {
    "ignore": [
      "*.test.js"
    ]
  },
  "dependencies": {
    "@metamathstudios/redis-wrapper": "1.0.4",
    "cluster": "^0.7.7",
    "dotenv": "^16.0.1",
    "ethers": "^5.6.6",
    "os": "^0.1.2"
  },
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "git-commit-msg-linter": "^4.1.2",
    "jest": "^28.1.0",
    "standard": "^17.0.0"
  }
}
